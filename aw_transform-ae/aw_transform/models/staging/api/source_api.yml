version: 2

sources:
  - name: source_api
    description: "Source from API for staging models"
    database: ted_dev
    schema: dev_yasmim_lima
    tables:

      - name: sales_order_detail
        identifier: raw_api_salesorderdetail
        description: "staging table for sales order detail"

      - name: sales_order_header
        identifier: raw_api_salesorderheader
        description: "staging table for sales order header"

      - name: purchase_order_detail
        identifier: raw_api_purchaseorderdetail
        description: "staging table for purchase order detail"

      - name: purchase_order_header
        identifier: raw_api_purchaseorderheader
        description: "staging table for purchase order header"