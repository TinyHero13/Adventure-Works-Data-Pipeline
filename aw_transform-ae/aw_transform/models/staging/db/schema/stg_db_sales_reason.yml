version: 2

models:
  - name: stg_db__sales_reason
    description: "Staging model for sales reason data"
    columns:
      - name: sales_reason_id
        data_type: int
        description: "Primary key for the sales reason"

      - name: name
        data_type: varchar
        description: "Reason for sales"
